import React from 'react';
import ReactDOM from 'react-dom/client';
import './fracomplete.css';
import Fracomplete from './App';

const root = ReactDOM.createRoot(document.getElementById('fracomplete'));
root.render(
  <React.StrictMode>
    <Fracomplete />
  </React.StrictMode>
);
